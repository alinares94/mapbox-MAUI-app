<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:map="clr-namespace:MauiBlazorApp.BlazorControls.Map"
             x:Class="MauiBlazorApp.MauiPage"
             Title="MauiPage" BackgroundColor="White">
    <Grid Padding="30" RowDefinitions="Auto, *">
        <Label Text="This text is a a XAML control, but the control below is BLAZOR!" FontSize="Body"
            VerticalOptions="Center" FontAttributes="Bold"
            HorizontalOptions="Center" />

        <map:MapControl Grid.Row="1"></map:MapControl>
    </Grid>
</ContentPage>